import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    checkbox: {
        flex: 1,
        width: 10,
        height: 10,
        backgroundColor: "red",
    },
});

export default styles